import React from 'react';
import {View, Text} from 'react-native';

export default () => {
  return (
    <View
      style={{backgroundColor: 'blue', height: 50, justifyContent: 'center'}}>
      <Text style={{color: 'white'}}>Header</Text>
    </View>
  );
};
