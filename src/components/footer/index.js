import React from 'react';
import {View, Text} from 'react-native';

export default () => {
  return (
    <View
      style={{
        backgroundColor: '#ccc',
        height: 50,
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text>Footer</Text>
    </View>
  );
};
